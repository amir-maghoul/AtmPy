# environment.yml
name: atmpy
channels:
  - conda-forge
  - defaults
dependencies:
  - cuda
  - numpy
  - numba
  - scipy
  - matplotlib
  - pytest
  - black
  - flake8
  - sphinx
  - pip
  - pip:
      - -e .
